project Ada_Interpreter is

   package Compiler is
      for Default_Switches ("ada") use ("-gnato", "-fstack-check", "-g", "-gnata");
   end Compiler;

   for Main use ("interpreter.adb");

end Ada_Interpreter;

